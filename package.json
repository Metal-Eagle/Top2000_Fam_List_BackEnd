{
  "name": "top2000_fam_list",
  "version": "1.0.0",
  "description": "",
  "author": "Metal-Eagle",
  "main": "src/app.js",
  "private": true,
  "scripts": {
    "start": "node src/app.js",
    "dev": "docker-compose -f docker-compose.dev.yml up -d --build && nodemon src/app.js",
    "runDocker:dev": "docker-compose -f docker-compose.dev.yml up --build",
    "runDocker": "docker-compose up"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "express": "^4.16.1",
    "helmet": "^4.4.1",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^3.1.11"
  }
}
